# Remove the SOC memory feature entirely

## Scope (confirmed with user)
Delete the `packages/soc-memory` package, all host/policy/profile wiring, tests, docs references — **and** delete the stored runtime data at `~/.dsh/memories/`. Clean up the stale plugin symlink and approval settings.

## 1. Delete the package
- `git rm -r packages/soc-memory/` (13 tracked files: lib/index.js, store.js, tenant.js, web.js, browser.js, automation.js, types, tests, README, package.json, cordis.patch.yml).
- Remove `"@citic/soc-memory": "workspace:*"` from `apps/soc-agent/package.json` dependencies.
- Regenerate the harness workspace lockfile: run `pnpm install` in `vendor/deepseek-harness/` (removes the `@citic/soc-memory` importer links from `pnpm-lock.yaml`; profile boots use `--frozen-lockfile`).

## 2. Host wiring (`apps/soc-agent/host.js`)
- Remove imports: `MEMORY_READ_TOOLS, MEMORY_WRITE_TOOLS` from policy.js; the entire `packages/soc-memory/lib/tenant.js` import block; the `store.js` imports (`detectSecrets`, `normalizeTags`, `validateContent`).
- Remove from re-exports and internals: `MEMORY_TOOLS` set, `validateMemoryExecution()`, exported `bindMemoryContext()`/`clearMemoryContext()`, the `socMemoryContext` service creation/`ctx.provide` block, the `MEMORY_TOOLS.has(...)` validation branch in `tools/pre-execute`, the memory branch in the approval-reason ternary, and the `agent/disposed → memoryContext.clear` handler (keep the `session/disposed` handler).
- Reword the deny message to drop "SOC memory" ("…only approved Splunk, Zimbra, subscription, scheduling, and catalog tools.").
- Note: stale `soc_memory_*` entries in saved auto-approve settings are already ignored by `actionSet()` once the tools leave `ACTION_TOOLS`.

## 3. Other host modules
- `auth-host.js`: remove the `socMemoryContext` tenant lookup in the `mcp/request-meta` handler; keep `soc_customer_id` in the metadata with an empty-string value (Python side already tolerates it) so the metadata shape stays stable.
- `ownership.js`: drop `'/_dsh/memory/settings'` from `PRIVATE_HTTP_PATHS` and from the admin-only path check.

## 4. Policy (`apps/soc-agent/policy.js`)
- Remove `soc_memory_search`, `soc_memory_read` from `READ_ONLY_TOOLS`.
- Remove the three `SOC memory` entries from `ACTION_CATALOG`.
- Delete `MEMORY_READ_TOOLS` and `MEMORY_WRITE_TOOLS` exports.
- Count deltas (verified against current tree): `READ_ONLY_TOOLS` 43→41, `ACTION_TOOLS` 32→29, `DOMAIN_TOOLS` 75→70, `READ_ONLY_DOMAIN_TOOLS` 37→35.

## 5. Profile registration
- Delete the `soc-memory` plugin block from `apps/soc-agent/cordis.patch.yml`.
- Remove `"soc-memory"` from `apps/soc-agent/tests/enabled-plugins.snapshot.json`.

## 6. Tests
- `apps/soc-agent/tests/policy.test.js`: update the four tool-count assertions to the new numbers; remove the five `soc_memory_*` entries from the APPROVAL_TOOLS deepEqual; remove the soc_memory membership assertions; remove the three soc_memory pre-execute assertions; delete the three memory-specific tests ("host binds memory tenant context…", "host context cleanup is automatic…", "host fails closed for cross-tenant memory arguments…"); remove the `bindMemoryContext`/`clearMemoryContext` import and the `createMemoryContextRegistry` import.
- `apps/soc-agent/tests/auth.test.js`: delete the `/_dsh/memory/settings` route-gating block inside the auth plugin test (the route no longer exists).
- Check `apps/soc-agent/tests/control-channel.test.js`, `investigation.test.js`, `skills.test.js` — currently clean per exploration; re-verify after edits.

## 7. setup.sh
- Remove the `@citic/soc-memory` line from the `verify_profile_resolution` spec list.
- Remove the `mem_link` symlink create/verify block in `ensure_soc_bundle`.
- Rewrite the profile-wiring comment that justifies the soc-memory resolution.

## 8. Runtime data + machine state (confirmed: delete)
- `rm -rf ~/.dsh/memories/` (memory_summary.md, archive/, audit.jsonl, diagnostics.json, rollout_summaries/, summary_history/).
- Delete the stale symlink `~/.dsh/profiles/web/node_modules/@citic/soc-memory`.
- Remove the stale `soc_memory_add/correct/forget` entries under `soc-action-approval.autoApproveActions` in `~/.dsh/settings.yaml`.

## 9. Documentation
- `AGENTS.md`: remove the entire `## Memory` section; drop "memory" from the untrusted-content sentence.
- `README.md`: reword the `packages` layout line to drop the memory package.
- `docs/PROJECT_STRUCTURE.md`: remove the soc-memory tree entry, the packages mention, and the `pnpm --filter @citic/soc-memory test` line.
- `benchmarks/README.md`: delete the inaccurate soc-memory "Postgres store" bullet.
- `docs/PERFORMANCE_REDESIGN_IMPLEMENTATION.md`: leave as a historical record (its memory mentions describe past phases); no edit.

## 10. Verification
1. `node --check` on host.js, auth-host.js, ownership.js, policy.js.
2. `pnpm install` in `vendor/deepseek-harness` regenerates `pnpm-lock.yaml`; confirm `git diff pnpm-lock.yaml` only removes soc-memory links.
3. Test suites: `apps/soc-agent` (`npm test` — includes the enabled-plugins snapshot rebuild), `packages/soc-agent-client`, `packages/soc-agent-scheduler` tests, and the Python suite (unaffected, but run to prove no hidden references).
4. `./setup.sh --plugins` — completes, does NOT recreate the soc-memory symlink, profile resolution check passes.
5. `grep -r "soc_memory\|soc-memory\|@citic/soc-memory\|socMemoryContext"` across the repo (excluding node_modules and the historical PERFORMANCE doc) → zero hits.
6. `./setup.sh --check` → "Everything required is set up."

Nothing is committed or pushed until you approve; after the removal you'll have one clean commit to review.
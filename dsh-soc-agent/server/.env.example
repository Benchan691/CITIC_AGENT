# Unified MCP server
MCP_SERVER_NAME=Splunk + Zimbra MCP
MCP_TRANSPORT=stdio
MCP_HOST=127.0.0.1
MCP_PORT=8050
LOG_LEVEL=INFO

# Authenticated webserver used for subscription management. The webserver
# sends subscription confirmation, update, and cancellation notifications.
EMAIL_SERVER_URL=http://100.114.50.103:9100
EMAIL_SERVER_TIMEOUT=30
EMAIL_SEVER_USER=
EMAIL_SEVER_PASSWORD=

# PostgreSQL stores the baseline Splunk/Zimbra settings and mailbox accounts.
# Non-empty values in this file override the stored settings at server startup.
APP_POSTGRES_URI=
APP_SETTINGS_ENCRYPTION_KEY=

# Initial or deployment override values for Splunk. Remove a value to fall back
# to the PostgreSQL setting or edit it in the DeepSeek Harness UI.
SPLUNK_HOST=splunk.example.com
SPLUNK_PORT=8089
SPLUNK_SCHEME=https
SPLUNK_URL=
SPLUNK_TOKEN=
SPLUNK_USERNAME=
SPLUNK_PASSWORD=
SPLUNK_VERIFY_SSL=true
SPLUNK_REQUEST_TIMEOUT=30
SPLUNK_JOB_TIMEOUT=120
SPLUNK_MAX_EVENTS=1000
SPLUNK_RISK_TOLERANCE=75
SPLUNK_SAFE_TIMERANGE=24h
SPLUNK_SANITIZE_OUTPUT=true
# Detection writes are disabled by default. Draft persistence requires the first flag;
# enabling a reviewed rule additionally requires the second flag.
SPLUNK_ALLOW_DETECTION_WRITE=false
SPLUNK_ALLOW_DETECTION_ENABLE=false
SPLUNK_DETECTION_APP=search
SPLUNK_DETECTION_OWNER=nobody

# Initial or deployment override values for Zimbra. Remove a value to fall back
# to the PostgreSQL setting or edit it in the DeepSeek Harness UI.
ZIMBRA_HOST=https://zmailbox.citictel-cpc.com/
ZIMBRA_VERIFY_SSL=false
ZIMBRA_TIMEOUT=60
ZIMBRA_MAX_ATTACHMENT_BYTES=10000000
ZIMBRA_MAX_ATTACHMENT_TEXT_CHARS=200000
ZIMBRA_ACCOUNTS_FILE=.data/zimbra_accounts.enc
ZIMBRA_ACCOUNTS_KEY_FILE=.data/zimbra_accounts.key
# Optional: derive the encryption key from this value instead of the key file.
ZIMBRA_ACCOUNTS_KEY=
# Optional single mailbox (alternative to the encrypted account store).
ZIMBRA_EMAIL=
ZIMBRA_PASSWORD=
# Write capability is off by default. Enable only when needed.
ZIMBRA_ALLOW_SEND=false
ZIMBRA_ALLOW_FILTER_WRITE=false
ZIMBRA_ALLOW_FILTER_REDIRECT=false
ZIMBRA_ALLOW_FILTER_DISCARD=false
ZIMBRA_ALLOW_FOLDER_WRITE=false

[citic_alert_delivery]
is_custom = 1
label = CITIC Alert Delivery
description = Send the original triggered alert result rows to the CITIC SOC backend.
alert.execute.cmd = citic_alert_delivery.py
python.version = python3
payload_format = json
# The action reads the original triggered result file as CSV.  Splunk may
# gzip that file; the action detects gzip by its magic header.
param.result_format = csv
param.results_file =
param.search_name =
passauth = 0
param.deployment =
param.spl =
param.app =
param.owner =
param.source_indexes =
param.selected_columns =
param.stable_id =
param.registration_id =
param.policy_id =
param.policy_revision =
param.definition_revision =
param.row_filters =
param.max_stored_rows =
